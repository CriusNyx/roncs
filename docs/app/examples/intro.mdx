## !!steps C#

Example source code in C#

```cs ! george.cs
// Here is what our C# object looks like
AIBehavior behaviorTree = new Sequence{
  children = [
    new FindFood{
      variableName = "food"
    },
    new NavigateTo{
      target = "food"
    },
    new Acquire{
      target = "food"
    }
  ]
}

Ron.Serialize(behaviorTree);
```

## !!steps Ron

* Easy to read
* Supports types and comments

```rs ! george.js
// AI behavior tree
Sequence(
  children: [
    FindFood(
      variableName: "food"
    ),
    NavigateTo(
      target: "food"
    )
    Acquire(
      target: "food"
    )
  ]
)
```

