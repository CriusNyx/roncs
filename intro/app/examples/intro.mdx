## !!steps Ron

* Easy to read
* Supports types and comments

```ts ! george.js
// An AI behavior tree represented in RON.
Sequence(
  children: [
    FindFood(
      variableName: "food"
    ),
    NavigateTo(
      target: "food"
    ),
    Acquire(
      target: "food"
    )
  ]
)
```

## !!steps C#

Example source code in C#

```cs ! george.cs
// The same AI Behavior tree represented in C#.
AIBehavior behaviorTree = new Sequence{
  children = [
    new FindFood{
      variableName = "food"
    },
    new NavigateTo{
      target = "food"
    },
    new Acquire{
      target = "food"
    }
  ]
};
```